#!/bin/sh

docker-compose up -d


#usermode -aG docker dimon
#usermod -aG docker dimon
#sudo usermod -aG docker dimon
#sudo docker-compose up -d
#sudo echo '127.0.0.1 example.dev' >> /etc/hosts
#sudo sudo echo '127.0.0.1 example.dev' >> /etc/hosts
#sudo echo '127.0.0.1 example.dev' >> /etc/hosts
#sudo bash -c "echo '127.0.0.1 example.dev' >> /etc/hosts"
#docker ps
#sudo docker ps
#sudo docker exec -ti 9b2a bash
#sudo docker-compose stop
#whois 188.225.18.125
#docker-compose up -d
#sudo 
#sudo docker-compose up -d
#docker ps
#sudo docker ps
#sudo docker exec -ti 9b2a bash